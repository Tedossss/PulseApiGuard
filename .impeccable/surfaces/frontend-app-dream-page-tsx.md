---
version: 1
slug: "frontend-app-dream-page-tsx"
primary_target: "frontend/app/dream/page.tsx"
related_targets: ["frontend/app/dream/DreamEvidenceExperience.tsx","frontend/app/dream/DreamEvidenceExperience.module.css","frontend/app/dream/DreamDepthField.tsx","frontend/app/dream/DreamDepthField.module.css"]
---

# Dream Evidence Atlas surface

- Scope: `/dream`; visitor mode: Experience; direction: Dream Evidence Atlas Overdrive.
- Audience/job: recruiters, hiring managers, and prospective clients must qualify Nazar Falach as a Poland-based full-stack engineer, understand the range of his work, inspect a relevant production record, and reach a direct next action quickly.
- Product truth: six evidence-backed project files lead to live products, repositories, case discussion, email, Telegram, and CV. Preserve the verified claims in code; never fabricate customers, endorsements, benchmarks, completed AI training, or production outcomes.
- First viewport, wide animated desktop: preload the PulseGuard landscape and keep identity/location, the centered Dream/Evidence switch, Contact, the scope brief, “Six verified case files,” availability, PulseGuard identity/premise, its full evidence file, Focus Trace, next-case cue, top progress, and all six rail destinations visible without interaction.
- First viewport, compact: below `1180px` availability may drop; at/below `760px`, retain name, strong work-scope line, PulseGuard identity/premise, Contact, view switch, and numeric rail while intentionally hiding location and the brief's supporting count/availability spans.
- Desktop journey: one `600svh` scroll track drives a single `100svh` sticky, full-bleed stage. Six absolute image stations cross-fade at viewport-centered markers; monumental project copy stays left and a physical case file floats upper-right. This replaces the obsolete permanent 64/36 split screen.
- Evidence state: the default floating file exposes padded index/status, Role, Stack, Record, three verified checks, and one or two actions. It uses warm paper, an offset backing sheet, irregular clipped edges, project-specific tilt, metal clip, NF seal, ledger line, and directional shadow.
- Dream state: on animated desktop the file compacts to index/status/title/role plus Inspect evidence at the lower right, while the image and decorative depth field open wider. It changes presentation only; it does not navigate or remove the semantic record from static layouts.
- Focus Trace: a pointer-inert, `aria-hidden` dashed SVG connects the active evidence row to each project's stored image anchor, ending at a halo and separate numbered paper pin. Hover may change the traced row; factual text remains in the list.
- Atlas rail: keep six equal fixed bottom cells, two-digit indexes, desktop titles, active Focus Yellow route progress, `aria-current="step"`, and scroll-to-station behavior. At mobile sizes it becomes a full-width `54px` numeric strip with minimum `52px` cells.
- Static journey: reduced motion or a viewport at/below `760px` moves all six semantic articles into document flow and removes scroll-marker choreography. Wider reduced-motion views use full-height 58/42 world/record pairs; mobile uses a `62svh` image (`500px` floor) followed by an overlapping near-full-width file. Both view modes retain complete evidence.
- Decorative depth: `DreamDepthField` is one lazy, client-only Three.js canvas with six translucent wireframe stations and a curved route. Mount it only above `760px`, without reduced motion, and when `(hover: hover) and (pointer: fine)` matches; compact, reduced-motion, and coarse-pointer paths omit it. It caps DPR at `1.35`, pauses while hidden, handles context loss, disposes resources, and fails silently behind complete DOM content.
- Accessibility: preserve the skip link, semantic landmarks and articles, labelled evidence sections, definition lists, live project announcement, `aria-pressed` switch, active-only tab order in animated mode, visible `3px` Focus Yellow outlines, forced-colors fallback, and direct contact links.
- Performance: keep the depth field out of SSR and out of ineligible device paths; preload only the first world image; use image assets and semantic HTML as the visual/factual fallback; do not make WebGL, pointer motion, backdrop blur, or hover essential.
- Approved comp: `.impeccable/mocks/decision/panoramic-atlas.png`.
- Component grammar: full-bleed cobalt worlds; floating cut-paper files; translucent navy chrome; tracked grotesk labels; large serif landmarks; physical clips/seals; Focus Trace; numbered anchors; a six-cell atlas rail; one restrained incident-orange signal.
- Unresolved: source WebP environments may soften on high-density ultra-wide displays. Do not upscale, fabricate screenshots, or substitute invented evidence to compensate.
